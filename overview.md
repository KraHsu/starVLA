# 毕设项目概览：starVLA × V-JEPA 时序融合

> 这份文档不是 TODO，而是**项目章程**。它要回答的不是"要做哪些事"，而是"为什么这件事值得做、要回答什么问题、怎么算做完了、与已有工作的边界在哪里"。读完之后再去执行 `implementation_todo.md`，每一个 checkbox 才有意义。

---

## 1. 一句话总览

**把 V-JEPA2.1 当作一个冻结的"短视频时序记忆模块"，通过离线特征缓存与轻量 projector 接入 starVLA 的视觉 token 流，在 LIBERO 上系统验证时序表征对 VLA 长时程操控与少样本学习的实际收益，并以受控消融把这个收益归因到"时序"训练目标本身，而非"自监督"或"模型规模"。**

---

## 2. 背景：两条平行线为什么应该汇合

### 2.1 VLA 这条线一直在变强，但视觉端基本是"单帧的"

近两年的 VLA（OpenVLA / OpenPI / SmolVLA / starVLA 等）核心进步集中在三件事：更大的 VLM backbone、更精细的 action head（FAST、OFT、Flow-Matching）、更广的机器人数据。这条线的视觉输入几乎都是**当前帧 + 多视角 + 状态**的静态组合；时序信息要么完全靠"过去几步动作 token"隐式承载，要么靠 action chunk 的时间维度被动获得。

这带来一个结构性弱点：**VLA 看不到"事情正在怎么变化"**。"杯子正在被推到边缘"和"杯子已经稳定在边缘"在单帧上几乎完全一样，但需要的下一步动作完全不同。

### 2.2 V-JEPA 这条线一直在做"时序表征"，但还没真正进入 VLA

V-JEPA / V-JEPA2 / V-JEPA2.1 的训练目标本身就是在 latent space 预测被遮蔽的未来时空块，这等价于学习"什么变化是控制相关的"。社区目前主要把 V-JEPA2 用作**世界模型做 planning**（V-JEPA2-AC、JEPA-WMs 那条线），而它作为**时序感知前端 / 短记忆编码器**接到现成 VLA 的尝试，还非常稀少。

### 2.3 这毕设站的位置

不去碰 VLA 主干、不去重新训世界模型，而是**把 V-JEPA 当成一个开箱即用的时序特征提供者**，研究"加它和不加它差多少、为什么差"。这是一个**实证研究 + 工程整合**问题，对本科毕设是合适的尺度——既不冒"重训基础模型"的险，也不只是"调一调 baseline"的空洞。

---

## 3. 核心研究问题

整篇毕设最终要回答三个问题，**且这三个问题各自有独立的实验来回答**。

### RQ1：时序视觉特征对 VLA 操控有用吗？

> 在保持 VLA 主体完全不变的前提下，仅通过冻结的 V-JEPA 特征注入，VLA 在 LIBERO 上的成功率、长时程稳定性、少样本效率是否得到实际提升？

回答它的实验：阶段 4 主实验（A vs C）。

### RQ2：收益是来自"时序"，还是来自"任意一种强自监督表征"？

> 如果把 V-JEPA 替换成 DINOv2、CLIP 等同等规模的图像自监督表征（同样池化、同样 projector），收益是否仍然存在？差距有多大？

这个问题非常关键。如果替换后收益几乎一样，那论文的故事就要改成"任何强视觉表征都能帮 VLA"；如果只有 V-JEPA 显著有效，那论文的故事就坐实在"时序训练目标"这个点上。

回答它的实验：阶段 5 的"上游表征替换"消融。**这是整篇毕设最具决定性的一组实验**。

### RQ3：怎样的工程接入方式既轻又有效？

> 在"离线缓存 + 轻量 projector"的极简路线下，融合算子（concat tokens / FiLM / cross-attn）、时序 token 数 \(K\)、历史窗口 \(N\)、是否冻结主干，分别对最终性能与系统开销有什么影响？

回答它的实验：阶段 5 的其余消融。

> **三个 RQ 各自独立，任意一个有结论都能写文章。** 这是这个题目可以"中途砍掉一半也能毕业"的根本原因。

---

## 4. 方法论：三段式实验设计

### 4.1 三组对照不是任意挑的

| 组别 | 内容 | 在论证链条中的角色 |
|---|---|---|
| **A. Baseline** | 原版 starVLA-OFT，单帧输入 | RQ1 的"零点"。回答"不加任何时序信息时模型多强" |
| **B. Naive temporal** | 把最近 4 帧的 ResNet-18 / CLIP 池化特征拼到视觉 token 流 | **关键对照**。回答"是不是只要给一点点时序信息 VLA 就能涨" |
| **C. V-JEPA 融合** | 我们的方法 | RQ1 的"主张" |

如果只有 A 和 C，审稿人会立刻问："你的提升到底是因为 V-JEPA 强，还是因为你给了 4 帧而不是 1 帧？" B 组就是为了堵这个问题。**B 必须存在**，否则 A vs C 的结果在论证上是单薄的。

### 4.2 三档数据量不是任意挑的

\[
\text{data\_fraction} \in \{10\%, 25\%, 100\%\}
\]

- **10%**：用来露出"少样本时差距更大"的趋势——这是冻结表征类方法最容易出彩的区间。
- **25%**：用来宣称"用 25% 数据做到 100% 数据的 95%"这种 sample-efficiency 卖点。
- **100%**：用来证明"哪怕数据充足也不会亏"。

如果你时间紧，**只跑 25% 和 100% 也能讲完整个故事**，但 10% 档很便宜（数据少训练快），所以默认保留。

### 4.3 评测套件的优先级

\[
\text{libero\_goal} \;\succ\; \text{libero\_10} \;\succ\; \text{libero\_spatial},\, \text{libero\_object}
\]

- `libero_goal`：目标驱动型任务，最适合验证"时序/记忆"假设。
- `libero_10`：长时程任务，最容易展现"时序稳定性"收益。
- 其余两套件：完整四套件评测时再补。

### 4.4 受控的消融比"全网格"更重要

阶段 5 的六组消融全部在 100% 数据 `libero_goal` 上各跑一个 seed，**不追求全网格** \(K \times \text{fusion} \times N \times \dots\)。原因很简单：这是本科毕设、不是顶会工作，每个维度独立扫一次足以支持论证，全交叉网格只会把时间烧光。

---

## 5. 系统设计：为什么基于 starVLA fork

### 5.1 starVLA 的几个属性恰好为这个题目设计

| starVLA 既有能力 | 在本毕设里的作用 |
|---|---|
| Lego-like 模块边界（model / dataloader / trainer / config 解耦） | 只新增文件就能挂上 V-JEPA，不动主干 |
| `--trainer.freeze_modules` 正则冻结 API | 一行配置就能冻结整个 VLM backbone |
| 按模块分组学习率 | projector 高 LR、主干低 LR 是默认能力，不用自己写 |
| `**/bar/` git-ignore 约定 | 个人实验脚本不污染仓库 |
| Florence-2 等小 VLM 支持 | 让 8×4090 是可行的，不需要 H20/H200 |
| 已有 WM4A（World Model for Action）先例 | 证明"外挂世界模型/视频模型到 VLA"在 starVLA 里是被设计接受的，不是hack |
| 已有 LIBERO 训练评测样例 | 阶段 1 的 baseline 工程量大幅压缩 |

### 5.2 V-JEPA 走 submodule 的理由

V-JEPA2 是 facebookresearch 的研究 repo（路径风格 `from src.x.y import ...`、内部脚本不打包，`pip install` 没有意义），用 submodule 引入到 `third_party/vjepa2` 是版本可锁、可读源、改动可追踪的稳定路线。它的依赖栈也与 starVLA 不完全一致（自带 transformers / timm / decord 的旧版 pin），需要 `uv pip install -e third_party/vjepa2 --no-deps` 装到主 venv `.venv`（py3.11，与 starVLA 共栈），才不会把训练栈的版本回滚掉。

更重要的工程意图是把 V-JEPA forward **完全踢出训练循环**：阶段 2 抽取一次特征落到 `npz/parquet`，阶段 3 起训练只读缓存。这意味着**进入主实验阶段后，训练 env 不再需要 V-JEPA 运行时依赖**——对算力受限的研究者有直接价值（4090 训练机不必再装 V-JEPA 2）。

### 5.3 整体数据流

\[
\underbrace{\text{LIBERO video clip}}_{\text{N=8 frames}} \xrightarrow{\text{V-JEPA2.1 (frozen)}}\; z \in \mathbb{R}^{D} \;\text{or}\; Z \in \mathbb{R}^{K\times D}
\]

\[
\xrightarrow{\text{offline cache}}\; \texttt{(episode\_id, frame\_id)} \to z \;\xrightarrow{\text{projector}}\; \tilde z \in \mathbb{R}^{H_\text{vla}}
\]

\[
\xrightarrow{\text{fusion (concat / FiLM / cross-attn)}}\; \text{VLA hidden states} \;\xrightarrow{\text{action head}}\; a_t
\]

整个链条里**只有 projector 和 fusion 这两块是可训练的新增参数**，其他全部冻结或共享 starVLA 原有训练 schedule。

---

## 6. 创新点（诚实版）

不夸张地说，这不是"算法层面的发明"，而是**实证 + 工程整合**层面的贡献。下面四点按重要性排序。

### 6.1 首个系统化 V-JEPA × VLA 实证研究

V-JEPA2 发布以来，社区主要把它用作**世界模型**做 planning（V-JEPA2-AC、JEPA-WMs 路线）。把它当作**时序感知前端**直接挂到现成 VLA 的视觉流上、并系统对比有/无的研究，目前几乎是空白。这个题目的核心定位就是去填这个空白。

### 6.2 受控消融解耦"时序"与"自监督"两个变量

这是整篇毕设最有方法论价值的一点。已有"加了某种自监督特征 VLA 涨了"这类报告几乎都没有控制"是不是任意一种自监督表征都行"。本毕设的 **V-JEPA vs DINOv2 vs CLIP**（同 projector、同融合算子、同数据）这一组消融，正面回答了"V-JEPA 的优势是不是来自它特有的时序训练目标"。这个问题不论结果正负，都是值得发表的。

### 6.3 极简、可移植的工程接入路线

提出并验证一条"**离线缓存 + 冻结主干 + 轻量 projector**"的路线，证明在不动 VLA 任何核心权重的前提下，**只增加少于 1% 的可训练参数**就能给 VLA 带来时序感知能力。这条路线对算力受限的研究者（高校、社区）有直接可复用价值。

### 6.4 可复用的 starVLA 插件

产出的 `vjepa_encoder.py`、`vjepa_projector.py`、`QwenOFT_VJepa.py` 三个文件以及对应 config，构成一组**可插拔组件**：换掉里面的特征抽取器，就能把任何视频/图像编码器接到 starVLA。这意味着这次毕设的工程产物天然支持"下次想试 DINOv3 / Hiera / VideoMAE"——这一点对答辩展示是有分量的。

> **不要声称的事**：不要说"我们提出了新的世界模型"、"我们 SOTA 了 LIBERO"、"我们的方法适用于所有 VLA"。这些都不是这毕设要做的事。诚实地把它定位成"一次结构清晰的实证研究"是更稳的策略。

---

## 7. 预期贡献清单

把上面四个创新点拆成**实际能交付的东西**：

| 类别 | 交付物 |
|---|---|
| 科研结论 | RQ1 / RQ2 / RQ3 三个问题各一个清晰回答（哪怕回答是"差距不显著"也算结论） |
| 数据产物 | LIBERO 上的 V-JEPA 特征缓存（公开则可被复用） |
| 模型产物 | 三组对照 × 三档数据量 × 3 seed 的 27 个 checkpoint + 消融 6 个 checkpoint |
| 代码产物 | starVLA fork 中三份新增模块文件 + 一份 config + 一份复现脚本 |
| 文档产物 | 主表 + 数据效率曲线 + 消融表 + 失败分析图 + Rollout 视频 |

---

## 8. 不做什么（明确边界）

为了让"做完"这件事是可定义的，**以下事情都明确不做**，并且会在论文 Limitations 一节写清楚：

- **不做真实机器人实验**。所有结果都在 LIBERO 仿真上。
- **不做 OpenPI 主线整合**。OpenPI 留作"未来工作"中提及，不进入实验。
- **不做基于 V-JEPA2-AC / JEPA-WMs 的 action reranker**。除非阶段 4 全部按时完成且还有余裕，否则砍。
- **不做 V-JEPA 自身的 post-training**。V-JEPA 全程冻结，不微调。
- **不追 LIBERO SOTA**。比较的对照永远是"同 backbone、同训练 schedule 的 baseline"，不是排行榜上的最强方法。
- **不做跨数据集泛化**。例如 DROID、RoboCasa 等都不做；这些只在相关工作章节提及。

> 把"不做什么"写在最前面，是为了在和导师讨论范围、在答辩 Q&A 时，能立刻把对方拉回到该评估的指标上，而不是被"你为啥不做 X"带偏。

---

## 9. 论文/答辩叙事骨架

整篇毕设可以浓缩成下面这一段叙事弧线，**论文章节、答辩开场、向导师汇报都可以共用**：

> "现有 VLA 在视觉端基本是单帧的，对时序变化不敏感，这在长时程任务里尤其明显（**问题**）。V-JEPA 系列以预测未来 latent 为训练目标，天然学习了控制相关的时序结构；但它目前主要被当作世界模型，几乎没人系统验证它能否当 VLA 的时序前端（**机会**）。我把 V-JEPA2.1 通过离线特征缓存 + 轻量 projector 接到 starVLA 的视觉 token 流上，不修改任何主干权重；在 LIBERO 上以三组对照 × 三档数据量验证收益，并通过把 V-JEPA 替换为 DINOv2 / CLIP 的关键消融，把收益归因到时序训练目标本身（**方法**）。结果显示在 \(\text{libero\_10}\) 长时程任务和 25% 数据档上，V-JEPA 融合显著优于 baseline 与等规模图像表征替换；融合算子上 \(\langle\text{...}\rangle\) 最优；这一路线只引入约 \(\langle X\%\rangle\) 可训练参数（**结果**）。这表明，**冻结的强时序表征是 VLA 在不动主干的情况下提升长时程稳定性与少样本效率的一条工程上极轻、方法论上可控的路径**（**结论**）。"

把这段话背下来。它就是答辩开场 90 秒。

---

## 10. 风险与备选叙事

最后这一节回答一个尖锐问题：**如果实验结果不顺，毕设还能怎么讲？**

| 实际结果 | 备选叙事 |
|---|---|
| C 显著优于 A 与 B | 主叙事，按上面骨架讲 |
| C 略优于 A，但与 B 差距不大 | 改讲"任意时序信号都能帮 VLA"——RQ2 的负面回答本身也是一篇论文 |
| C 与 A 几乎持平 | 改讲"V-JEPA 时序特征与 VLA 视觉流之间的对齐难题"——把可解释性章节扩成主章节，分析为什么没涨 |
| C 在某些任务涨、某些任务跌 | 改讲"任务依赖型分析"：哪些任务靠时序、哪些任务靠空间，给一个分类标准 |
| 训练完全跑不通 | 退到 SmolVLA + 单套件 + 仅 RQ3 的工程消融，仍然能写一篇"V-JEPA 接入 VLA 的工程可行性研究" |

**这个题目的安全余量来自三个 RQ 互相独立**：哪怕 RQ1 失败，RQ2 和 RQ3 仍然可以单独支撑一份毕设。这就是为什么我建议你从一开始就严格按这三个 RQ 来组织实验和写作，而不是先冲一个"V-JEPA 一定能赢"的主观目标。

---

## 11. 一张图记住整篇文档

```
            ┌────────────── RQ1：时序特征对 VLA 有用吗？─────────┐
            │                                                  │
            │     A. baseline      B. naive temporal       C. V-JEPA 融合
            │     单帧             4 帧池化               冻结 V-JEPA + projector
            │                                                  │
            └──────────────────────────┬───────────────────────┘
                                       │
                                       ▼
                  ┌─ RQ2：收益是"时序"还是"自监督"？─┐
                  │                                  │
                  │   V-JEPA  vs  DINOv2  vs  CLIP   │   ← 关键消融
                  │   （同 projector、同融合算子）   │
                  │                                  │
                  └──────────────┬───────────────────┘
                                 │
                                 ▼
                ┌─ RQ3：哪种接入方式最划算？─┐
                │                            │
                │  fusion / K / N / freeze   │   ← 工程消融
                │                            │
                └────────────────────────────┘
                                 │
                                 ▼
              （冻结主干 + 离线缓存 + 轻量 projector）
                  starVLA fork 上的最小侵入实现
```

---

## 12. 与 TODO 的关系

读完这份文档，再去看 `implementation_todo.md`，应该出现下面这种对应关系：

- **TODO 阶段 0–3** 都是为了让 RQ1、RQ2、RQ3 能被"测得出来"。
- **TODO 阶段 4** 直接回答 RQ1。
- **TODO 阶段 5** 直接回答 RQ2 和 RQ3。
- **TODO 阶段 6** 是为可解释性章节积累素材，**也是 RQ1 失败时的备胎主线**。
- **TODO 阶段 7** 是把上述结论收口成可交付物。

**任何一项 TODO 子任务，如果你不能把它对应到上面 12 节中的某一节，那它就是冗余的，应该砍掉。** 这是这份文档对 TODO 的最大约束作用。
